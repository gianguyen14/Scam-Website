[project]
name = "backend"
version = "0.1.0"
description = "Scam Detector Backend Phase 0"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.29.0",
    "pydantic>=2.6.4",
    "redis>=5.0.0",
    "apscheduler>=3.10.1",
    "requests",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.1.1",
    "httpx>=0.27.0",
]
